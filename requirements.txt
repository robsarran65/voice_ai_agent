# Local dev installs from THIS file (pip install -r requirements.txt).
# Vercel's build ignores it entirely and uses pyproject.toml's
# [project.dependencies] instead (its uv-based installer doesn't fall back
# to requirements.txt once pyproject.toml exists at all). Keep both lists
# in sync when adding or bumping a dependency.
fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
python-dotenv==1.1.1
litellm==1.98.0

# Weather lookups (wttr.in, Open-Meteo fallback)
httpx==0.28.1

# Google Calendar + Gmail
google-api-python-client==2.198.0
google-auth==2.49.2
google-auth-oauthlib==1.4.0
